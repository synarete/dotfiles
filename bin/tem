#!/usr/bin/env bash
export LC_ALL=en_US.UTF-8
export LANG=en_US.UTF-8
export LANGUAGE=en_US.UTF-8
export COLORTERM=truecolor
export TERM=xterm-256color
export ESHELL="/bin/bash"
command -v emacs-nw || (echo "emacs-nw is missing" && exit 1)
emacs-nw "$@"
